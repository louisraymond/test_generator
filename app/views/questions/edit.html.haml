.form-page
  .form-page__header
    %h1 Edit question
    %p Adjust the prompt, update the answer, or re-align the learning outcomes.
  = form_with(model: @question) do |form|
    = render 'form', form: form, question: @question
