<%# Real implementation in commits 4.2 / 4.4 / 4.8.
    Stub so the spike spec doesn't need composite plumbing. %>
<div class="q__cm-composite-placeholder">
  Composite editor pending — see commits 4.2+.
</div>
