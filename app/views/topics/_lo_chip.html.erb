<span class="topic-detail__chip <%= chip_class %>"
      data-topic-module-target="chip"
      data-questions="<%= q_count %>"
      data-usage="<%= u_count %>"
      title="<%= chip_title %>"
      aria-label="<%= chip_label %>"><%= chip_text %></span>
