<!-- placeholder for #55 (sub-55) — locals: { mod:, exam_usage:, idx: } -->
<%# Minimal stub so sub-53's _topic_main can render. sub-55 overwrites this. %>
<section id="mod-<%= mod.id %>" class="topic-detail-v2__module-card" data-module-id="<%= mod.id %>">
  <h2 class="topic-detail-v2__module-card__title" tabindex="-1"><%= mod.name %></h2>
</section>
